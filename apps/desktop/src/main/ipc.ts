// TODO(detail-impl): ipcMain.handle / webContents.send 桥接
import type { MainWindow } from "./window";
import type { NativeAddon } from "./native";

export function registerIpcHandlers(_main: MainWindow, _native: NativeAddon): void {
  // TODO(detail-impl)
}
